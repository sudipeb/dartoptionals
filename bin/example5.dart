void main(){
  String? name;
   print(name ?? 'sudip');

  String? surname;
  final bla = surname??name; // optional string data type as the input source is from the 

  String? college;
  print(name ?? 'kathford');
  print(college ?? name);
}